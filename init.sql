-- This will create the database schema for the local PostgreSQL instance

-- Create extensions if needed
-- Intentionally left empty to avoid SQL Server (MSSQL) linter errors in VS Code.
-- For PostgreSQL, run this once in your DB shell if not already installed:
--   CREATE EXTENSION IF NOT EXISTS pgcrypto;
