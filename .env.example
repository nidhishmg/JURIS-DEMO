# Copy this file to .env and fill in the real values before running locally
# Example Postgres connection string (adjust user, password, host, port, dbname)
# DATABASE_URL=postgres://username:password@localhost:5432/juris_db

# Application settings
PORT=5000
NODE_ENV=development

# Run without calling any external LLM/API providers
# When true, the server returns deterministic local placeholders for chat/analysis,
# and uses pseudo-embeddings for RAG.
OFFLINE_MODE=true

# OpenAI / OpenRouter keys
# OPENAI_API_KEY=sk-...
# OPENAI_API_MAIN=

# Replit/OIDC settings (only needed if using Replit auth flow)
# REPLIT_DOMAINS=http://localhost:5000
# REPL_ID=
# ISSUER_URL=

# Session secret for express-session (use a long random value in production)
# SESSION_SECRET=some_long_secret

# Storage (S3) keys if using uploads
# S3_KEY=
# S3_SECRET=
# VECTOR_DB_KEY=
